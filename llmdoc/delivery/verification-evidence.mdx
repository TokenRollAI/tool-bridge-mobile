---
description: 可复用的验证命令、证据记录模板、场景覆盖与历史证据失效规则。
kind: guide
relations:
  requires:
    - delivery/definition-of-done.mdx
    - delivery/evidence-language.mdx
  related:
    - delivery/engineering-baseline.mdx
    - delivery/github-preview-release.mdx
    - integration/sdk-device-transport.mdx
code:
  paths:
    - package.json
    - scripts/**
    - test/**
    - src/**/__tests__/**
    - .github/workflows/**
---

# 验证证据工作法

## 证据记录模板

每次验证至少记录：

- 被验证的 commit/worktree 状态、范围和变更类型；
- 命令原文及 Node、pnpm、Expo、Java/Xcode、平台和 runner 版本；
- App variant、application id/bundle id、build 类型、设备/模拟器型号与 OS；
- Gateway/storage 版本、环境和能力 path；secret、完整 URL/query、正文、坐标与媒体必须脱敏；
- 成功/失败/阻塞、退出码、关键断言、artifact 名称/大小/digest 与可追溯地址；
- 证据直接覆盖的 DOD 条目，以及明确未覆盖的平台、真机、后台、锁屏、弱网、物理效果或服务端语义。

结果采用“已验证 / 未验证 / 受阻”三段式。受阻必须写清外部服务、工具链或上游契约缺口；失败和未运行
不能归类为成功。日期化记录可以证明某个历史 commit/artifact，但不能作为当前工作树的持续事实。

## 2026-09-03：SDK 0.21.0 Android Preview 构建、安装与冷启动

### 已验证

- 当前 PR 在 commit `406899dd2b1faa75f5b29502e172418de466c39a` 使用仓库锁定的 Node 22.23.1、
  pnpm 11.21.0 执行 `pnpm build:android:preview`。clean prebuild 与 Gradle `assembleRelease` 成功，
  总耗时 2m21s；Gradle 共 944 tasks（912 executed、32 up-to-date），Metro 打包 1,609 modules。
- 产物是 `android/app/build/outputs/apk/release/app-release.apk`，大小 134,685,919 bytes，SHA-256 为
  `a6e3c6210aa05ed425c608899bf2365e5878e37a42a8251411265a3582786b55`。产物反查得到 package
  `ai.tokenroll.toolbridgemobile.preview`、label `Preview`、versionName `0.0.13`、versionCode `13`，
  min/target/compile SDK 为 24/36/36；APK 内含 `assets/index.android.bundle` 与 `assets/app.config`，后者
  明确记录 `productionTransport: @tool-bridge/sdk/device@0.21.0`。
- `apksigner` v2 验证通过。签名是 Android Debug 测试证书，证书 SHA-256 为
  `fac61745dc0903786fb9ede62a962b399f7348f0bb6f899b8332667591033b9c`；该证据不能称为 production signer。
- 当时唯一连接设备为序列号 `4245df0c`、Xiaomi model `25102RKBEC`、Android 16，且已安装同一 Preview
  package。`adb install -r` 返回 `Success` 并完成保留数据的覆盖安装；设备 package manager 的
  `firstInstallTime` 仍为 `2026-08-23 21:11:16`，`lastUpdateTime` 更新为 `2026-09-03 18:06:52`。
- 显式 force-stop 后执行 `am start -W`，cold start 返回 `Status: ok`，Activity 为 `.MainActivity`，
  `TotalTime` 为 146ms；随后 Activity 为 top-resumed 且 App pid 存活。

### 未验证与限制

- 覆盖安装和冷启动只证明上述 APK 可在该 Android 设备保留安装身份并启动，不证明保留下来的本地数据
  内容或迁移语义正确，也不证明权限申请、拒绝、撤销或系统设置变化行为。
- 本次没有验证真实 Gateway、mailbox enqueue/claim/complete、后台、锁屏或任何具体 capability 的物理
  效果；install/launch 成功不能外推为这些 E2E 已通过。

## 2026-08-26：SDK 0.17 Android 后置相机与 Store 闭环

### 已验证

- 第一份 0.0.11 Android Preview APK 基于当时与下述第二份 APK 相同的源码完成 clean prebuild + release Gradle build，但构建
  Node 是不符合仓库锁定值的 v26.7.0。`aapt` 反查 APK 为
  `ai.tokenroll.toolbridgemobile.preview`、versionName `0.0.11`、versionCode `11`；`assets/app.config` 同时
  是 Preview、scheme `toolbridgemobile-preview`、`appVariant` 为 `preview`、`productionTransport` 为
  `@tool-bridge/sdk/device@0.17.0`。这证明此前 native Preview / JS Development 的 artifact 分裂已在该
  APK 修复；下述物理拍照与 Store 闭环只归属于这第一份 APK。
- 第一份 APK 通过 `adb install -r` 覆盖安装到序列号 `4245df0c`、model `25102RKBEC` 的 Xiaomi 真机并
  保留配置；调用时 App 为前台，Tool Bridge mount `device/phone/9daf921003a4` 为 online，control mode
  是 `direct_call`。
- 清空 logcat 后只发起一次新的副作用调用：

  ```sh
  tb call 'device/phone/9daf921003a4/camera/capture_photo' \
    --args '{"purpose":"验证 SDK 0.17 远程拍照与 Store 上传","facing":"back","quality":"low"}'
  ```

  调用 exit 0，返回 JPEG `960x1280`、11,569 bytes、SHA-256
  `ed963a13b1ebcc4b72b9fc73d8ff7243368419674dd4eb822a3f784da0b6371a` 与
  `store://default/cLa2joV--mLdCxHKew74smc-`。同一清空后的窗口记录 camera 0，
  `takePictureInternal` 为 02:23:31.157、`onImageCaptured` 为 02:23:31.380，并在 02:23:32.698 正常
  disconnect；这些事件只归属于该 02:23 新调用，不借用旧窗口日志。
- 对同一 Store URI 执行 `system/store/stat` 得到 `ready`、`image/jpeg`、11,569 bytes、producer
  `device:9daf921003a4`；随后通过受保护读取实际得到 11,569 bytes，重新计算 SHA-256 与相机 result 完全
  一致。照片不写入系统相册，因而 Gallery 没有新条目不构成失败。
- 随后用仓库锁定的 Node 22.23.1 对同一源码重新 clean build 第二份 Preview APK，SHA-256 为
  `94d2cf9d23a94368126f65c440a826b4a3eccb36ce4d8563ddef2c761a8b043d`；其 `aapt` package/version 与
  `assets/app.config` 身份均与第一份完全一致，并再次 `adb install -r` 成功。安装后立即发起的第一次
  `status/get` 因正常的短暂断连读取失败；3 秒后只读 `status/get` 恢复为 App `active`、mount online、
  control mode `direct_call`。这证明最终 Node 22 artifact 可构建、身份一致、可覆盖安装并恢复在线，
  不等于它已重复通过物理拍照/Store 闭环。

### 受阻

- `mise exec node@22.23.1 -- pnpm build:ios:sim` 已实际尝试；prebuild 创建 `ios/` 后，本机 PATH 没有
  CocoaPods，Expo 尝试自动安装未继续，随后 `xcodebuild` 因 active developer directory 是
  `/Library/Developer/CommandLineTools`、缺少完整 Xcode 而 exit 1。`command -v pod` 为空，Homebrew 也未
  安装 CocoaPods；因此是环境阻塞，不是 iOS build 成功，也不是未尝试。

### 未验证与限制

- 这次闭环只覆盖 Android Preview、后置镜头、已授 Camera 权限、前台、Direct call 和单个 Store 对象；
  不覆盖 iOS、前置镜头、首次授权/拒绝/撤销、Ask/Trusted UI、系统指示/快门声、后台/锁屏、弱网/重连、
  Store 拒绝/过期/保留或完整 command/device/MIME/size/TTL/checksum/single-use 强绑定。
- 最终 Node 22.23.1 APK 没有发起第二次相机副作用调用；不能把第一份同源码、同身份但由 Node 26.7.0 构建
  的真机拍照/Store 成功直接记为最终 artifact 的物理能力验收。
- SDK 0.17 `call.uploadObject` 的 options 不接收相机 controller 的本地 AbortSignal。前台丢失可阻止本地
  相机/处理继续和成功结果提交，但不能保证已经开始的 Store HTTP 上传被终止；本次成功不能补足该证据。
- 该历史现场 Gateway 0.17 的约 60 秒 call authority 会收紧移动端相机 120 秒本地 ceiling；本次约 2 秒的成功不验证
  接近 deadline 的确认、处理或上传行为。

## 2026-08-25/26：旧 SDK 0.15 Preview artifact 与上传失败

### 已验证

- 基于 `a2ea637` 的未提交修复工作树完成 Android preview release clean build，并用 `adb install -r`
  保留数据安装到 Xiaomi `25102RKBEC`；安装包为 `ai.tokenroll.toolbridgemobile.preview`
  0.0.9/versionCode 9。
- ADB 现场确认 App 为 resumed 前台、manifest 声明 Camera、runtime permission 已授予、AppOps 为
  foreground，`dumpsys media.camera` 可见 back/front Camera ID `[0,1]`。
- 2026-08-26 01:47 的另一个旧 APK camera 1 窗口含 `takePictureInternal`、非空
  `onImageCaptured`、3264x2448 JPEG 编码及 1920x2560 重编码；它证明该 artifact 能完成 Android 前置
  硬件成像和本地处理，但不是下述 01:57 调用的日志窗口。
- 01:57 清空后的独立窗口只把下述真实 Gateway call 与 camera 0 open/close session 关联起来，没有
  `takePictureInternal`；因此不能把 01:47 的详细成像日志归到 01:57 调用。
- 相机专项 Jest 为 4 suites / 16 tests passed，TypeScript typecheck 与 ESLint passed；覆盖 native facing
  adapter、front/back preflight、权限映射、处理和上传 consumer 的本地边界。

### 受阻

- 2026-08-26 01:57（Asia/Shanghai）的旧 APK 真实 Gateway call 在 create-upload 阶段因 Gateway 未挂载
  可写 `camera/photos` context 而失败；
  没有完成对象 PUT、稳定 `node://` result 或真实 Gateway/storage E2E，不能把此前的本地处理成功写成完整
  `capture_photo` 成功。该失败属于 0.0.9 / SDK 0.15 artifact，不能描述随后 0.17 Store 契约，更不能外推为
  当前 0.21.0 的 Gateway/Store 回归结果。
- iOS 只完成 clean prebuild。本机缺少 CocoaPods 和完整 Xcode，仅有
  `/Library/Developer/CommandLineTools`，因此未通过 `xcodebuild`，不能记为 iOS 原生 build。

### 未验证

- 在该旧 artifact 窗口中，iOS build/真机相机、Android 后置镜头、双端真机、真实 `camera/photos` 对象
  上传与受保护读取均未验证；
  首次授权/拒绝/设置撤销、后台/锁屏、弱网、系统相机指示/快门声和 crash 后孤儿清理也不能从本次证据
  外推。

## 仓库与逻辑验证

从锁定工具链和 frozen install 开始：

```sh
pnpm install --frozen-lockfile
pnpm verify
pnpm peers check
pnpm audit:dependencies
```

`pnpm verify` 串联文档链接、release/changelog、三变体配置、图标、原生模块、SDK device entry、secret、
license、dependency mitigation、Expo version、typecheck、lint 与 Jest。失败的子项不可被单独成功命令覆盖。

按范围补充：

- protocol/schema：官方上游 fixture、真实 SDK consumer 和 downstream/Gateway compatibility；
- persistence：真实 SQLite migration、并发/重放、crash recovery、容量与 retention；
- UI：component/accessibility、焦点/公告/大字号、取消和错误态；
- 网络/对象：redirect、timeout/cancel、MIME/signature、声明/实际字节、credential invalidation 和清理；
- 安全：permission/config、secret/redaction、control/bidi、abuse、revoke 与 unknown-result。

注入式 adapter、fake WebSocket 或 mocked fetch 只能证明本地边界和 consumer contract，不可命名为真实
Gateway E2E、对象存储兼容或真机行为。

## 原生生成与构建

Expo config、依赖、plugin、权限或 native module 变化时，至少执行对应的 clean prebuild/build：

```sh
APP_VARIANT=development pnpm exec expo config --type public
pnpm build:android:debug
pnpm build:ios:sim
```

Android 权限需检查三层：Expo config introspection、clean prebuild 的 merged manifest、安装后的
`dumpsys package`。`blockedPermissions` 可移除依赖声明，但 debug manifest 或其他高优先级 manifest 仍可能
重新引入；只看 config 不能下最终结论。iOS 同理检查 generated Info.plist、entitlements、background modes
和 usage descriptions。

clean prebuild/autolinking/Metro export 只证明生成配置或 bundle；Gradle/Xcode build 才证明对应原生工作树
可编译；模拟器 build 不证明真机签名、权限和物理行为。构建受网络或本机工具链阻塞时，记录已完成层级，
不要把 prebuild 提升为 native build。

## Android Preview APK

内部 Android 离线安装包使用 `pnpm build:android:preview`。验收至少确认：

- package 是 preview application id；
- APK 包含 `assets/index.android.bundle`，不依赖 Metro；
- 签名可验证并明确是否为测试 key；
- 显式 MainActivity 可冷启动；
- 记录 artifact SHA-256。

本地产物不自动成为 CI/Release artifact；preview test key 不能称 production signer。

## 真实 Gateway、对象存储与真机

Gateway matrix 要逐能力验证 hello/expose/call/cancel/result、schema/error、重复 id、deadline、credential
拒绝/revoke、断线重连和脱敏，不能用另一个能力的成功外推。`status/get` 的单次 Android 路径不覆盖 inbox、
camera、iOS、后台、弱网或长期稳定。

Store 对象上传另需验证 call-scoped capability、create-upload、relay PUT、checksum/size/MIME/TTL/
single-use、稳定 URI、受保护读取与保留；SDK mocked fetch、本地 filename/idempotency key 或一次真实对象
闭环都不等价于服务端完整强绑定。

push/mailbox 需验证 enqueue/claim/lease/cancel/expiry/result、token rotation/revoke、opaque wake hint、
未送达与进程终止恢复；本地 notification、timer、SQLite inbox 或前台长连不构成该证据。

双端真机按能力覆盖首次授权、拒绝、设置中撤销、前后台/锁屏/kill、低电量、弱网和物理效果。相机还要
覆盖可见预览、Direct 自动拍摄、系统指示/快门声、上传取消与临时文件；媒体覆盖锁屏/耳机/来电中断；
notification/timer 覆盖 channel、呈现、点击、Doze/reboot 或平台等价场景；accessibility 覆盖真实读屏器、
Switch Access 与大字号。

## Release 证据

每个 Preview release 记录 workflow 路径（main 自动或手工 tag）、verified SHA、tag、GitHub Release、APK
资产 URL 与独立核对的 SHA-256。workflow/release notes 来源及权限边界见
`delivery/github-preview-release.mdx`。

Production 还需签名 AAB/IPA、privacy/entitlement/store 声明、SBOM、安装 smoke、Gateway matrix、golden
scenario、监控、staged rollout 与 rollback；Preview 成功不能替代。

## 证据失效与复用

- 依赖、Expo/RN/SDK、原生 plugin、权限、schema/wire、Gateway/storage 或能力关键路径变化时，相关旧证据
  失效并重跑对应层级；
- 纯文档更新可引用未变化实现的最近有效证据，但仍需当前 `pnpm verify`；
- 历史 artifact 只对其 tag/commit 有效，不用于宣称当前工作树已 build 或已真机验收；
- Android 证据不外推 iOS，prebuild/Metro 不外推 native build，模拟器不外推真机，本地 contract 不外推
  Gateway，schedule promise 不外推 presented/delivered/clicked。
